# norix-flutter-plugin

A third-party plugin for Norix CLI to detect Flutter mobile applications.

## Installation

```bash
npm install ../examples/flutter-plugin
```

## Configuration

Add the plugin to your `norix.config.json` configuration file:

```json
{
  "plugins": ["norix-flutter-plugin"]
}
```
